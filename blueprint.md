
# Lotto Number Generator

This project is a simple web application that generates random lottery numbers for the user.

## Current State

*   **HTML (`index.html`):** Basic HTML structure.
*   **CSS (`style.css`):** Minimal styling.
*   **JavaScript (`main.js`):** Empty.

## Plan

1.  **Create the UI:**
    *   Add a title: "Lotto Number Generator".
    *   Create a container to display the generated numbers.
    *   Add a button to trigger the number generation.
2.  **Style the UI:**
    *   Center the content.
    *   Style the number container and the button.
3.  **Implement the logic:**
    *   Write a JavaScript function to generate 6 unique random numbers between 1 and 45.
    *   Display the generated numbers in the UI.
